# Example Environment Variables for Playwright Browser Agent
# Copy this file to .env and fill in the required values.

# --- Required ---

# Specify the LLM provider (e.g., openai, anthropic, google, ollama).
# See LiteLLM docs for supported providers: https://docs.litellm.ai/docs/providers
LLM_PROVIDER=openai

# Specify the model name for the chosen provider (e.g., gpt-4o, claude-3-opus-20240229, gemini/gemini-1.5-pro-latest).
LLM_MODEL=gpt-4o

# Provide the API key corresponding to the LLM_PROVIDER.
# Replace the key name and value based on your provider.
# Examples:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=AIza...
# OLLAMA_API_BASE=http://localhost:11434 # If using Ollama locally
OPENAI_API_KEY=YOUR_API_KEY_HERE

# --- Optional (Defaults are usually False or None) ---
# These can be set here, but CLI flags will override them.

# Run the browser in headless mode (true/false)
# HEADLESS=false

# Record screenshots after each action (true/false)
# RECORD=false

# Path to a JSON file containing additional model configuration (e.g., temperature)
# MODEL_CONFIG_PATH=./config/model_settings.json